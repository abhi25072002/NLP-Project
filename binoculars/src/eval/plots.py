# src/eval/plots.py

def plot_roc_curve(labels, preds, save_path=None):
    """
    Plot ROC curve.
    
    Args:
        labels (list): Ground truth labels.
        preds (list): Predicted probabilities.
        save_path (str): Path to save the plot.
    """
    pass
